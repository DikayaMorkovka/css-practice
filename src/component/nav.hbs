<nav>
    <ul class="nav {{#if isVertical}}nav_vertical{{else}}nav_inline{{/if}}">
        {{#each list}}
        <li><a class="nav_link" href="{{url}}">{{text}}</a></li>
        {{/each}}
    </ul>
</nav>